# plot twist, that bubble can't be crushed with default params, it's too long!
# so that test case isn't so interesting..
~/minia/build/bin/minia -in unitig_bubble -skip-bcalm -skip-bglue -redo-links 
